---
structure: 
source: book_zh/ods_zh_4_1.md
chapter: 4.1 基本结构
section: 4.1
page: 101
kind: textbook
---

# 4.1 基本结构

从概念上讲，跳表是一系列单向链表 L , . . . , L 。每个链表 L 包含 L 中
0 h r r 1
−
的一部分元素。我们从包含 n 个元素的输入列表 L 开始，并从 L 构造 L
0 0 1
，从 L 构造 L ，依此类推。L 中的元素是通过对 L 中的每个元素 x 投
1 2 r r 1
−
掷硬币来获得的，如果硬币正面朝上，则将 x 包含在 L 中。当我们创建
r
一个空的列表 L 时，这个过程结束。跳表的一个例子如图 4.1 所示。
r
对于一个跳表中的元素 x，我们称 x 的 height 为大 最值
L5
L4
L3
L2
L1
L0 0 1 2 3 4 5 6
sentinel
图 4.1：包含七个元素的跳表。
r 使得 x 出现在 L 中。因此，例如，只出现在 L 中的元素的高度为 0。
r 0
如果我们花点时间思考一下，我们会注意到 x 的高度对应于以下实验：反
复掷硬币，直到出现反面为止。正面朝上出现了多少次？答案并不奇怪，
节点的期望高度是 1。（我们预计在出现反面之前会掷两次硬币，但不计
算最后一次掷硬币。）跳表的 height 是其最高节点的高度。
在每个列表的开头都有一个特殊的节点，称为 sentinel，它作为列表的
哑节点。跳表的关键特性是存在一条从 L 中的哨兵到 L 中每个节点的短
h 0
路径，称为 search path。记住如何为节点 u 构建搜索路径很容易（见图 4.
2）：从跳表的左上角（L 中的哨兵）开始，总是向右移动，除非那会超
h
过 u，在这种情况下，你应该向下到下面的列表中移动一步。
更准确地说，要为节点 u 在 L 中构建搜索路径，我们从 L 中的哨兵
0 h
w 开始。接下来，我们检查 w.next。如果 w.next 包含出现在 L 中 u 之前
0
的项，则将 w 设为 w.next。否则，我们向下移动，并在列表 L 中 w 的
h 1
−
位置继续搜索。我们以这种方式继续，直到到达 L 中 u 的前驱。
0
以下结果，我们将在第4.4节中证明，表明搜索路径相当短：
引理 4.1. The expected length of the search path for any node, u, in L is
0
at most 2 log n + O(1) = O(log n).
一个空间-实现跳表的一种高效方法是定义一个节点 的，你，
L5
L4
L3
L2
L1
L0 0 1 2 3 4 5 6
sentinel
图4.2：跳表中包含4的节点的搜索路径。
由一个数据值 x 和一个指针数组 next 组成，其中 u.next[i] 指向 u 在列表
L 中的后继节点。通过这种方式，节点中的数据 x 只被引用一次，即使 x
i
可能出现在多个列表中。
SkiplistSSet
class Node<T> {
T x;
Node<T>[] next;
Node(T ix, int h) {
x = ix;
next = Array.newInstance(Node.class, h+1);
}
int height() {
return next.length - 1;
}
}
本章的接下来的两个部分讨论跳表的两种不同应用。在每种应用中，
L 存储主结构（元素列表或元素的有序集合）。这些结构之间的主要区别
0
在于搜索路径的导航方式；特别是，它们在决定搜索路径是否应该进入
L 还是在 L 内向右移动方面有所不同。
r 1 r
−

（中文关键词：跳表、数组）
